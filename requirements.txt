# verl framework (install from source)
# git clone https://github.com/volcengine/verl && cd verl && pip install -e .

# Core dependencies
torch>=2.1.0
transformers>=4.40.0
datasets
accelerate
hydra-core>=1.3.0
omegaconf
ray[default]>=2.10.0
numpy
pandas
pyarrow
Pillow

# Training
flash-attn>=2.5.0
peft

# Rollout
sglang>=0.3.0

# Reward server
fastapi
uvicorn
pydantic
requests
aiohttp

# Logging
wandb
